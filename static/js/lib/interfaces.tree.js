/*
 * Interfaces.tree.js, 负责控制tree的数据交互的接口
 * @author: freemen
 * @date: 2014-05-13
 * */

var iTree = {
	/*
	 * some setting
	 * */
	config: {}, 

	/*
	 *
	 * */
	

};

if ( hit.INTERFACES ) {
	hit.INTERFACES.tree = iTree;
} else {
	hit.INTERFACES = {
		tree: iTree
	}
}
