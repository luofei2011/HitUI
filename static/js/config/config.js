/**
 * @description 所有跟网站相关的配置请统一配置到这里
 * @author luofei & freemen
 * @date 2014-05-19
 */
if (typeof hit === "undefined")
	window.hit = {};
if (typeof hit.config === "undefined")
	hit.config = {};

hit.config = {
	poup_01: {
		
	}
};